{
  "name": "roorooroo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "start": "pnpm -r start",
    "frontend:dev": "pnpm --filter ./frontend dev",
    "backend:dev": "pnpm --filter ./backend dev"
  }
}
